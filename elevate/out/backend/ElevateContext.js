"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ElevateContext = void 0;
const FileSnapshot_1 = require("../backend/FileSnapshot");
class ElevateContext {
    // File snapshot with version numbers and full text before analysis
    snapshot;
    // For testing
    text;
    // Initial inputs
    // Text from VScode of active file after sanitization
    analysisTarget;
    // Pipeline outputs
    parsed;
    // Prompt generated by PromptBuilder to sender to Ollama
    // prompt?: Prompt obj;
    // Response from Ollama
    // modelResponse?: Response obj;
    // compiler error/warnings
    diagnostics;
    constructor(docOrText) {
        if (typeof docOrText === 'string') {
            this.text = docOrText;
        }
        else {
            this.snapshot = (0, FileSnapshot_1.snapshot)(docOrText);
        }
    }
}
exports.ElevateContext = ElevateContext;
//# sourceMappingURL=ElevateContext.js.map